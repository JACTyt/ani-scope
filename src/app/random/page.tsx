import { RandomPickClient } from '@/components/random/RandomPickClient';

export const metadata = { title: 'Random Pick — AniScope' };

export default function RandomPage() {
  return <RandomPickClient />;
}
